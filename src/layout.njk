<!doctype html>
<html lang="en" data-theme="light">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{{ title or "Solutions Log" }}</title>
  <meta name="description" content="{{ description or 'A log of the solutions and decisions I drive as an Engineering Manager — showcasing impact beyond code.' }}" />
  <link rel="icon" href="{{ '/assets/favicon.svg' | url }}" type="image/svg+xml">
  <meta name="robots" content="noindex, nofollow">
  <meta name="author" content="Reuben Ingber" />

  <!-- Pico Zinc -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.zinc.min.css">
  <!-- Your custom CSS -->
  <link rel="stylesheet" href="{{ '/assets/styles.css' | url }}" />
</head>
<body>
  <header class="site-header container">
  <div class="brand hero">
    <!-- Monochrome icon that inherits color -->
    <span class="logo logo-mask"
      aria-hidden="true"
      style="--logo-url: url('{{ '/assets/solutions-logo.svg' | url }}')"></span>

    <div class="hero-text">
      <h1 class="site-title">Solutions Log</h1>
      <p class="site-tagline">
        A log of the solutions and decisions I have driven as an Engineering Manager — showcasing my impact both in and beyond code.
      </p>
    </div>
  </div>
</header>

  <main id="main" class="container">
    {{ content | safe }}
  </main>

  <footer class="container site-footer">
    <div class="footer-copy">
      <small>© {{ "now" | date("yyyy") }} • Reuben Ingber, Engineering Manager</small>
    </div>
  </footer>
</body>
</html>
